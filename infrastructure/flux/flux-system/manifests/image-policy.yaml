apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: curs-flask-prod-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: curs-flask-repo
  policy:
    alphabetical:
      order: asc
