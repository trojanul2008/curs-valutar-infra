apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: curs-valutar-dev-autoupdate
  namespace: flux-system
spec:
  suspend: true
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: infra-dev
  git:
    checkout:
      ref:
        branch: dev
    commit:
      author:
        name: fluxcdbot
        email: fluxcdbot@example.com
      messageTemplate: "chore: update curs-valutar-app dev image to {{ .UpdatedImage }}"
  update:
    path: infrastructure/k8s/overlays/dev
    strategy: Setters
