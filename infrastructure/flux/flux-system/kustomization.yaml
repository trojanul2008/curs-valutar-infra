apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - manifests/source-dev.yaml          # GitRepository infra-dev (create the dev source)
  - manifests/external-secrets-install.yaml

  # Environment Kustomizations (these actually deploy workloads)
  - manifests/curs-valutar-dev.yaml
  - manifests/curs-valutar-prod.yaml

  # NOTE: Temporarily DISABLED until image controllers are installed
  - manifests/image-repository.yaml
  - manifests/image-policy-dev.yaml
  - manifests/image-policy.yaml
  - manifests/image-update-automation-dev.yaml
  - manifests/image-update-automation.yaml

  - manifests/flux-controllers.yaml
