# infrastructure/flux/flux-system/gotk-components.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infrastructure/flux/flux-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
    mode: client

