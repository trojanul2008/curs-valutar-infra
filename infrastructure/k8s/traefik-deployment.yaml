apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - name: traefik
        ports:
        - containerPort: 80
          hostPort: 31710   # Add this
        - containerPort: 443
          hostPort: 31877   # Add this
