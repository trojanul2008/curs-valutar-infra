apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod

resources:
  - ../../base

patches:
  - path: deployment-patch.yaml
  - path: ingress-patch.yaml
  - path: remove-affinity-patch.yaml   # ← add this

images:
  - name: trojanul2008/curs-flask
    newName: trojanul2008/curs-flask
    newTag: prod-latest

