apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev

resources:
  - ../../base
  - pdb.yaml

patches:
  - path: deployment-patch.yaml   # <— adaugă ăsta
  - path: ingress-patch.yaml

