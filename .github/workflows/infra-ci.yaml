name: "🚦 Infrastructure CI"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: infra-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: "Validate ${{ matrix.environment }} desired state"
    runs-on: ubuntu-latest
    timeout-minutes: 20

    strategy:
      fail-fast: false
      matrix:
        environment:
          - dev
          - prod

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install pinned CI tools
        run: scripts/install/install-ci-tools.sh

      - name: Lint YAML
        run: |
          find infrastructure .github \
            -type f \
            \( -name '*.yaml' -o -name '*.yml' \) \
            -print0 \
            | xargs -0 yamllint -c .yamllint.yml

      - name: Render Kubernetes manifests
        run: |
          mkdir -p artifacts

          kustomize build \
            "infrastructure/k8s/overlays/${{ matrix.environment }}" \
            > "artifacts/manifest-${{ matrix.environment }}.yaml"

          test -s "artifacts/manifest-${{ matrix.environment }}.yaml"

          echo "Rendered resources:"
          grep '^kind:' \
            "artifacts/manifest-${{ matrix.environment }}.yaml" \
            || true

      - name: Validate Kubernetes schemas
        run: |
          kubeconform \
            -strict \
            -summary \
            "artifacts/manifest-${{ matrix.environment }}.yaml"

      - name: Validate Kyverno policies
        run: |
          kyverno apply infrastructure/kyverno \
            --resource "artifacts/manifest-${{ matrix.environment }}.yaml" \
            --policy-report \
            | tee "artifacts/kyverno-${{ matrix.environment }}.log"

      - name: Validate Checkov policies
        shell: bash
        run: |
          set +e

          # CKV_K8S_43 is temporarily skipped because the application
          # image migration to immutable digest references is handled
          # separately from this repository-topology migration.
          checkov \
            -f "artifacts/manifest-${{ matrix.environment }}.yaml" \
            --framework kubernetes \
            --skip-check CKV_K8S_43 \
            -o json \
            > "artifacts/checkov-${{ matrix.environment }}.json"

          RC=$?

          set -e

          echo "Checkov summary:"

          jq '{
            passed: (.results.passed_checks | length),
            failed: (.results.failed_checks | length),
            skipped: (.results.skipped_checks | length)
          }' "artifacts/checkov-${{ matrix.environment }}.json"

          if [[ "$RC" -ne 0 ]]; then
            echo
            echo "Checkov failed checks:"

            jq -r '
              .results.failed_checks[]? |
              "\(.check_id) | \(.check_name) | \(.resource)"
            ' "artifacts/checkov-${{ matrix.environment }}.json"

            exit "$RC"
          fi

      - name: Trivy configuration report
        run: |
          trivy config \
            --severity MEDIUM,HIGH,CRITICAL \
            --exit-code 0 \
            "artifacts/manifest-${{ matrix.environment }}.yaml" \
            | tee "artifacts/trivy-config-${{ matrix.environment }}.txt"

      - name: Trivy image vulnerability report
        shell: bash
        run: |
          mkdir -p artifacts/trivy-images

          mapfile -t IMAGES < <(
            yq -r '.. | .image? | select(.)' \
              "artifacts/manifest-${{ matrix.environment }}.yaml" \
              | sort -u
          )

          if [[ "${#IMAGES[@]}" -eq 0 ]]; then
            echo "No container images found."
            exit 1
          fi

          for IMAGE in "${IMAGES[@]}"; do
            SAFE_NAME="$(
              printf '%s' "$IMAGE" \
                | tr '/:@' '____'
            )"

            echo "Scanning $IMAGE"

            trivy image \
              --severity HIGH,CRITICAL \
              --exit-code 0 \
              --no-progress \
              --format json \
              --output "artifacts/trivy-images/${SAFE_NAME}.json" \
              "$IMAGE"
          done

      - name: Security summary
        if: always()
        shell: bash
        run: |
          {
            echo "## ${{ matrix.environment }} infrastructure validation"
            echo

            FILE="artifacts/checkov-${{ matrix.environment }}.json"

            if [[ -f "$FILE" ]]; then
              PASSED="$(jq '.results.passed_checks | length' "$FILE")"
              FAILED="$(jq '.results.failed_checks | length' "$FILE")"

              echo "### Checkov"
              echo "- Passed: $PASSED"
              echo "- Failed: $FAILED"
              echo
            fi

            echo "### Trivy"
            echo "HIGH/CRITICAL image vulnerabilities are currently reported but are not yet a blocking CI gate."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload validation artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: infrastructure-ci-${{ matrix.environment }}
          path: artifacts/
          retention-days: 14
          if-no-files-found: warn
