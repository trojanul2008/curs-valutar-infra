---
name: Infra Validation
on:
  push:
    branches:
      - dev
      - prod
  pull_request:
    branches:
      - dev
env:
  KUSTOMIZE_VERSION: v5.0.2
  KUBECONFORM_VERSION: 0.6.4
  KYVERNO_VERSION: v1.12.4
  CHAOS_MESH_VERSION: v2.6.1
jobs:
  lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: 3.x
      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install yamllint
        run: pip install yamllint
      - name: Lint YAML
        run: |
          find infrastructure -name '*.yaml' -o -name '*.yml' \
            | xargs yamllint -c .yamllint.yml
  schema:
    name: Schema Validation
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - name: Install Kustomize
        run: >
          curl -sL \
            "https://github.com/kubernetes-sigs/kustomize/releases/download/\
             kustomize%2F${{ env.KUSTOMIZE_VERSION }}/kustomize_${{ env.KUSTOMIZE_VERSION }}_linux_amd64.tar.gz" \
            -o kustomize.tar.gz
          tar xzf kustomize.tar.gz

          sudo mv kustomize /usr/local/bin/
      - name: Install Kubeconform
        run: >
          curl -sL \
            "https://github.com/yannh/kubeconform/releases/download/\
             v${{ env.KUBECONFORM_VERSION }}/kubeconform-linux-amd64.tar.gz" \
            -o kubeconform.tar.gz
          tar xzf kubeconform.tar.gz

          sudo mv kubeconform /usr/local/bin/
      - name: Validate base
        run: |
          kustomize build infrastructure/k8s/base | kubeconform -verbose
      - name: Validate dev overlay
        run: |
          kustomize build infrastructure/k8s/overlays/dev | kubeconform -verbose
      - name: Validate prod overlay
        run: >
          kustomize build infrastructure/k8s/overlays/prod | kubeconform -verbose
  policy:
    name: Kyverno Policy Check
    runs-on: ubuntu-latest
    needs: schema
    steps:
      - uses: actions/checkout@v4
      - name: Install Kyverno CLI
        uses: kyverno/action-install-cli@v0.2.0
        with:
          release: ${{ env.KYVERNO_VERSION }}
      - name: Test Kyverno policies
        run: >
          kustomize build infrastructure/k8s/base > base-manifests.yaml

          kyverno apply infrastructure/kyverno/ \
            --resource base-manifests.yaml \
            --output json > kyverno-results.json || true
          if jq -e '.failed | length > 0' kyverno-results.json; then
            echo "❌ Kyverno policy violations detected:"
            jq -r '.failed[] | "RESOURCE: \(.resource.kind)/\(.resource.name) | POLICY:
              \(.policy) | RULE: \(.rule)"' kyverno-results.json
            exit 1
          else
            echo "✅ All Kyverno policies passed"
          fi
  scan:
    name: Image Vulnerability Scan
    runs-on: ubuntu-latest
    needs: policy
    if: github.ref != 'refs/heads/dev'
    steps:
      - uses: actions/checkout@v4
      - name: Install Trivy
        run: >
          sudo apt-get install -y wget apt-transport-https gnupg lsb-release

          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -

          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" \
          | sudo tee /etc/apt/sources.list.d/trivy.list

          sudo apt-get update

          sudo apt-get install -y trivy
      - name: Scan container images
        run: >
          mkdir -p .github/scripts

          cat <<'EOF' > .github/scripts/scan-images.sh

          #!/bin/bash

          set -euo pipefail


          find infrastructure -name '*.yaml' -exec grep -E 'image: ' {} \; \
            | awk '$2 != "" && $2 !~ /PLACEHOLDER/ {print $2}' \
            | sort | uniq \
            | while read -r image; do
                echo "Scanning $image"
                trivy image --cache-dir /tmp/trivy-cache --exit-code 1 --severity HIGH,CRITICAL "$image"
              done
          EOF

          chmod +x .github/scripts/scan-images.sh

          mkdir -p /tmp/trivy-cache

          ./.github/scripts/scan-images.sh
  drift:
    name: Drift Detection
    runs-on: ubuntu-latest
    needs: scan
    steps:
      - uses: actions/checkout@v4
      - name: Install Flux CLI
        run: |
          curl -s https://fluxcd.io/install.sh | sudo bash
      - name: Check for configuration drift
        run: |
          kustomize build infrastructure/k8s/overlays/dev > dev-manifests.yaml
          kustomize build infrastructure/k8s/overlays/prod > prod-manifests.yaml
          flux diff kustomization curs-valutar-dev \
            --path infrastructure/k8s/overlays/dev \
            || echo "No drift in dev"
          flux diff kustomization curs-valutar-prod \
            --path infrastructure/k8s/overlays/prod \
            || echo "No drift in prod"
