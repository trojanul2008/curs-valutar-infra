name: "Install yq"
description: "Installs yq using version from versions.yaml"

runs:
  using: "composite"
  steps:
    - name: Run yq installer
      shell: bash
      run: |
        chmod +x ${{ github.action_path }}/install.sh
        ${{ github.action_path }}/install.sh
