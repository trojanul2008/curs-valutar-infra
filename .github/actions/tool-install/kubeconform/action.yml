name: "Install kubeconform"
description: "Installs kubeconform using version from versions.yaml"

runs:
  using: "composite"
  steps:
    - name: Run kubeconform installer
      shell: bash
      run: |
        chmod +x ${{ github.action_path }}/install.sh
        ${{ github.action_path }}/install.sh
