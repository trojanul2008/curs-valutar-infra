name: "Install kubectl"
description: "Installs kubectl using version from versions.yaml"

runs:
  using: "composite"
  steps:
    - name: Run kubectl installer
      shell: bash
      run: |
        chmod +x ${{ github.action_path }}/install.sh
        ${{ github.action_path }}/install.sh
